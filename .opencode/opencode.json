{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["AGENTS.md"],
  "permission": {
    "bash": {
      "npm install*": "ask",
      "pip install*": "ask",
      "git push*": "ask",
      "git reset*": "ask",
      "git push --force*": "deny",
      "rm -rf*": "deny",
      "git clean -fdx*": "deny"
    }
  }
}
